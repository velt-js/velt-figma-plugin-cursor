# Builder STRICT FIX — vision crop packets

Phase: `/Users/mayankpagar/Documents/Plugin Workspace/velt-figma-plugin-cursor/golden/judge-validation-fixture/cold-start-run`
Doctrine: each P0 is a human demo-fix. **Read both PNGs** before editing.
Fix top packets first. Do not grind P2 deltas while any P0 remains.
Dispatch by route mode — structure rows are NOT CSS fixes.

## P0 #1 of 1 — `composed.flow.cold-start-magenta-stripe`

- **Block:** flow
- **Miss:** injected magenta diagnostic stripe visible on panel chrome — novel cold-start defect
- **Provenance:** vision
- **Route:** replan — uncertain root cause — replan; never guess with CSS

OPEN THESE TWO IMAGES (Read tool):
1. LIVE (broken):  `/Users/mayankpagar/Documents/Plugin Workspace/velt-figma-plugin-cursor/golden/judge-validation-fixture/cold-start-run/judge-evidence/composed.flow.cold-start-magenta-stripe/live.png`
2. DESIGN (target): `/Users/mayankpagar/Documents/Plugin Workspace/velt-figma-plugin-cursor/golden/judge-validation-fixture/cold-start-run/judge-evidence/composed.flow.cold-start-magenta-stripe/figma.png`

Do what a human demo-fix does:
- Look at both crops
- Probe the live DOM for the nodes in the crop
- Apply minimal fix in the routed mode (mechanism CSS / wireframe / host prop)
- Re-screenshot the SAME crop box
- Done only when live crop matches design crop on template chrome
  (ignore dummy vs real names/text/timestamps)

