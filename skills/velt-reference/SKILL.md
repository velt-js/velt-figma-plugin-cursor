---
name: velt-reference
description: Look up an exact Velt identifier — slot, prop, CSS variable/class, wireframe variable, hook, API, event, feature flag. Use whenever you need a real name; never invent one (R10).
---

Identifier lookup (R10 — if it isn't in the reference, it doesn't exist).

- Start at the Surface lookup in `guide/reference/component-catalog.md` (per-surface: primitive · root wireframe · key props · flags · variables). Then the exhaustive pages in `guide/reference/`: `component-definitions.md`, `wireframe-components.md`, `wireframe-variables.md`, `wireframe-tokens.md`, `primitives.md`, `props.md`, `component-config.md`, `css-variables.md`, `css-classes.md`, `feature-flags.md`, `hooks.md`, `apis.md`, `events.md`.
- Return the exact verified name. To confirm a class on a rendered element, inspect with `shadowDom={false}`.
