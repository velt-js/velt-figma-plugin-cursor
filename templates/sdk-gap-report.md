# Velt SDK-gap report

> Honest map of where the design needed something the SDK can't customize cleanly today. Each row was confirmed via `guide/sdk-gaps-and-blockers.md` (not a hack, not a wrong-layer miss). This is the artifact to hand back to Velt.

**Run:** `{runId}` · **Guide version:** `{guideVersion.sha}`

| Surface | Requirement | Why no clean path | Attempted layer(s) | Closest clean alternative (shipped) | Suggested SDK addition | Guide ref |
|---|---|---|---|---|---|---|
| {surface} | {what the design needed} | {specific limitation + rule/ref} | {css/wireframe/primitive/headless} | {what shipped instead} | {smallest capability that'd make it possible} | {guide page} |

_No rows = every design need was met cleanly._
